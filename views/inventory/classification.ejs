<!-- Title and Sort Button -->
<div id="classification-header">
    <% if (title) { %>
        <h1><%= title %></h1>
    <% } else { res.redirect('/') } %>
        <button id="sortBtn" type="button">
            <span class="sortBtn-large">Sort Vehicles</span>
            <span class="sortBtn-small">Sort</span>
        </button>
</div>

<!-- Sort/Filter Menu (hidden by default) -->
<form id="sort-inventory-form" method="get">
    <h2>Sort & Filter Vehicles</h2>
        <!-- Order -->
        <div class="sort-form-section">
            <label for="order">Order:</label>
            <select name="order" id="order">
                    <option value="newest_listing">Newest Listing</option>
                    <option value="oldest_listing">Oldest Listing</option>
                    <option value="lowest_price">Lowest Price</option>
                    <option value="highest_price">Highest Price</option>
                    <option value="name_asc">Name (A-Z)</option>
                    <option value="name_desc">Name (Z-A)</option>
                    <option value="newest_vehicle">Newest Vehicles</option>
                    <option value="oldest_vehicle">Oldest Vehicles</option>
            </select>
        </div>
        <!-- Price Filter -->
        <div class="sort-form-section">
            <input type="checkbox" id="enablePrice" name="enablePrice">
            <label for="enablePrice"><strong>Price:</strong></label>
            <input type="number" name="minPrice" id="minPrice" placeholder="Min" step="1000" min="0" disabled>
            <input type="number" name="maxPrice" id="maxPrice" placeholder="Max" step="1000" min="0" disabled>
        </div>
        <!-- Miles Filter -->
        <div class="sort-form-section">
            <input type="checkbox" id="enableMiles" name="enableMiles">
            <label for="enableMiles"><strong>Miles:</strong></label>
            <input type="number" name="minMiles" id="minMiles" placeholder="Min" step="1000" min="0" disabled>
            <input type="number" name="maxMiles" id="maxMiles" placeholder="Max" step="1000" min="0" disabled>
        </div>
        <span id="sortError" style="color:red;"></span>
        <div class="sort-inventory-form-buttons">
            <button type="submit">Apply Search Settings</button>
            <button type="button" id="closeSortMenu">Cancel</button>
        </div>
</form>

<!-- Message Block (will be added later) -->
<%# messages() %>

<!-- Inventory Items Display -->
<%- grid%>

<script src="/js/classification.js"></script>